# Pokemon Arena (Coming Soon)

The Pokemon arena will use [poke-env](https://poke-env.readthedocs.io/) to run
Pokemon Showdown battles inside sandbox pods.

Each sandbox pod will run a local Showdown server + the user's agent vs a built-in opponent.

**Status:** Planned. Requires bundling Pokemon Showdown (Node.js) into the sandbox image.
